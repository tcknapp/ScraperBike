<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Scraper Bike</title>
   
    <!--JS/Other files-->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
     <!-- Latest compiled and minified CSS & JS -->
    <link href="https://maxcdn.bootstrapcdn.com/bootswatch/4.0.0/flatly/bootstrap.min.css" rel="stylesheet">
    {{!-- <link rel="stylesheet" href="css/styles.css"> --}}
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js"></script>
</head>

<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
        <div class="container">
            <a class="navbar-brand" href="/">Start Scraping...</a>
            <ul class="nav navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="/notes">Saved</a>
                </li>
            </ul>
        </div>
    </nav><br><br><br>

    <!-- Page Content -->
    <div class="container">

        <!-- Jumbotron Header -->
        <header class="jumbotron jumbotron-fluid text-center">
            <h1 class="display-3 text-center">Scraper Bike</h1>
            <p class="lead">A Scraper for Current Music News</p>
            <a href="/scrape" class="btn btn-primary btn-md">Scrape</a>
        </header>

        <!-- Scraped articles here -->
        <div class="row text-center">

            <div class="col-lg-12 col-md-12 mb-12">
                <div class="card">
                    {{#each article}}
                    <img class="card-img-top" src="" alt="">
                    <div class="card-body">
                        <h4 class="card-title">
                            {{this.title}}
                        </h4>
                        <p class="card-text">Summary</p>
                        <a href="{{this.link}}" class="btn btn-link">Link</button>
                    </div>
                    <div class="card-footer">
                        <a href="#" class="btn btn-primary" id ="saveBtn">Save</a>
                        <a href="#" class="btn btn-primary" id="commentBtn">Comment</a>
                    </div>
                </div>
                {{/each}}
            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer class="py-2 bg-dark fixed-bottom">
        <div class="container">
            <p class="m-0 text-center text-white">TK 2018</p>
        </div>
    </footer>

     <script src= "../js/app.js"></script>


</body>

</html>